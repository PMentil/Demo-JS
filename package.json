{
  "name": "prueba-git",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rimraf dist && parcel ./src/index.html --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "parcel": "^2.7.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
